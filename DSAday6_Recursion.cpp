#include<bits/stdc++.h>
using namespace std;
/*void fact_recursion_pr(int i,int fact){
    if(i<1){
        cout<<fact;
        return ;
    }
    fact_recursion_pr(i-1,fact*i);
}
int fact_recursion_fun(int n){
    if(n==0) return 1;
    return n*fact_recursion_fun(n-1);
}
void sum_recursion_pr(int i, int sum){
    if(i<1){
        cout<<sum;
        return ;
    }
    sum_recursion_pr(i-1,sum+i);
}
int sum_recursion_fun(int n){
    if(n==0){
        return 0;
    }
    return n+sum_recursion_fun(n-1);
}
int recursion_fib(int n){
    if(n<1)return 1;
    return recursion_fib(n-1)+recursion_fib(n-2);
}*/
int main(){
//print the sum of first N natural no.s with the help of recursion
int n;
cout<<"Enter the no.";
cin>>n;
// sum_recursion_pr(n,0);
// cout<<endl;
// cout<<sum_recursion_fun(n)<<endl;
// fact_recursion_pr(n,1);
// cout<<endl;
// cout<<fact_recursion_fun(n);

    return 0;

}
